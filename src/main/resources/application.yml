management:
  endpoints:
    web:
      exposure:
        include: health,info

codeops:
  demo:
    mode: synthetic

management:
  tracing:
    sampling:
      probability: 1.0

management:
  tracing:
    sampling:
      probability: 1.0
